{
  "name": "cutisiq-root",
  "private": true,
  "scripts": {
    "build": "npm install --prefix \"version 1\" && npm run build --prefix \"version 1\"",
    "dev": "npm run dev --prefix \"version 1\"",
    "start": "npm start --prefix \"version 1\"",
    "lint": "npm run lint --prefix \"version 1\""
  }
}
